
function objectInvert(sampleObject) {
  // const invertedObject = {};
  // const invertFunction = key => invertedObject[sampleObject[key]] = key;
  // Object.keys(sampleObject).map(invertFunction);
  return sampleObject;
}

export {
  objectInvert,
};
