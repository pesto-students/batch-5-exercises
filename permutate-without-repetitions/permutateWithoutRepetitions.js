
function permutateWithoutRepetitions(sampleArray) {
  // const permuatatedArray = [];
  // const permuteRecusiveFunc = function(array, acc = []) {
  //   if (array.length === 0) {
  //     permuatatedArray.push(acc)
  //   }
  //   permuatatedArray = sampleArray.map(
  return sampleArray;
}

export {
  permutateWithoutRepetitions,
};
